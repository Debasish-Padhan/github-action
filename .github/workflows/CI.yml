name: CI

on:
  push:
    branches:
      - develop
      - uat
    tags:
      - v*

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run develop script
        id: develop-script
        if: github.ref_name == 'develop'
        run: |
          echo Hello, Development!
          echo ---
          echo Ref_name, ${{ github.ref_name }}
          echo Ref, ${{ github.ref }}
          echo "ENV_DATA=development" >> "$GITHUB_OUTPUT"

      - name: Copy data
        if: github.ref_name == 'develop'
        env:
          ENV_DATA: ${{ steps.develop-script.outputs.ENV_DATA }}
        run: |
          echo Hello, Development!
          echo ---
          echo Ref_name, ${{ github.ref_name }}
          echo Ref, ${{ github.ref }}
          echo data $ENV_DATA 
      
      - name: Run uat script
        if: github.ref_name == 'uat'
        run: |
          echo Hello, UAT!
          echo ---
          echo Ref_name, ${{ github.ref_name }}
          echo Ref, ${{ github.ref }}
          
      - name: Generate App Version Name
        if: startsWith(github.ref_name, 'v')
        run: VERSION_NAME=$(git tag -l | cut -c2-)
      
      - name: Run production script
        if: startsWith(github.ref_name, 'v')
        run: |
          APP_VERSION=$(git tag -l | cut -c2-)
          echo Hello, Production-${{ env.VERSION_NAME }}!
          echo Hello, Production-${{ env.APP_VERSION }}!
          echo Hello, Production-$(git tag -l | cut -c2-)!
          echo ---
          echo Ref_name, ${{ github.ref_name }}
          echo Ref, ${{ github.ref }}
